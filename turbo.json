{
  "$schema": "https://turbo.build/schema.json",
  "globalEnv": ["REYNA_SERVER_PORT"],
  "pipeline": {
    "build": {
      "dependsOn": ["^build"],
      "outputs": ["dist/**", ".next/**"]
    },
    "lint": {
      "outputs": []
    },
    "dev": {
      "cache": false
    }
  }
}
